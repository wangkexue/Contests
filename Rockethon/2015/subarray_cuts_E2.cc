#include <cstdio>
#include <algorithm>
#include <cmath>

using namespace std;

int main() {
	int N, K, i, j;
	scanf("%d%d", &N, &K);
	int nums[N];
	for (i = 0; i < N; i++)
		scanf("%d", &nums[i]);
	
	printf();
}